#include <stdio.h>
#include "min.h"


int main(){
    int indexMin = 0;
    int len = 0;
    printf("Please, add the size of the array");
    scanf("%d", &len);

    printf("Please, add the elements of the array\n");
    int arr[len];
    // resolver como cachar cada elemento de array
        //Code: for(i--> arr[i]);
    //////

    // min.h///
    for (int i = 0; i < len; i++){

        if(arr[i] > arr[indexMin]){

        };
    };

    ////

    // print  min.h
    return 0;
};